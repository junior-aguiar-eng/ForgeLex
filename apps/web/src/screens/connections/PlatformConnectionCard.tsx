import React from 'react';
import { CheckCircle2, CircleDashed } from 'lucide-react';
import { connectionStateLabel, isVerifiedConnection, platformName, type PlatformConnection } from './connection-model';

export const PlatformConnectionCard: React.FC<{ connection: PlatformConnection; selected: boolean; onSelect: () => void }> = ({ connection, selected, onSelect }) => {
  const verified = isVerifiedConnection(connection.state);
  return <section className={`surface space-y-4 p-5 transition-colors ${selected ? 'border-cognac-500 ring-2 ring-cognac-700/10' : ''}`}><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Host de IA</p><h2 className="mt-1 font-editorial text-2xl font-bold text-stone-900">{platformName(connection.platform)}</h2></div><span className={`inline-flex items-center gap-1.5 rounded-full border px-2.5 py-1 text-xs font-semibold ${verified ? 'border-emerald-200 bg-emerald-50 text-emerald-800' : 'border-stone-200 bg-stone-100 text-stone-600'}`}>{verified ? <CheckCircle2 className="h-3.5 w-3.5" aria-hidden="true" /> : <CircleDashed className="h-3.5 w-3.5" aria-hidden="true" />}{connectionStateLabel(connection.state)}</span></div><p className="text-sm leading-relaxed text-stone-600">Use o host que já faz parte do seu trabalho. O ForgeLex fornece a infraestrutura jurídica; o modelo e a assinatura pertencem ao host.</p><button type="button" onClick={onSelect} aria-pressed={selected} className={selected ? 'btn-primary w-full justify-center' : 'btn-secondary w-full justify-center'}>Mostrar instruções para {platformName(connection.platform)}</button></section>;
};
