import React from 'react';
import { Check, Copy } from 'lucide-react';
import { isLocalMcpUrl, platformName, type PlatformConnection } from './connection-model';

export const ConnectionChecklist: React.FC<{ connection: PlatformConnection; copied: boolean; onCopyUrl: () => void }> = ({ connection, copied, onCopyUrl }) => {
  const localUrl = isLocalMcpUrl(connection.mcpUrl);
  return <section className="surface space-y-5 p-6 sm:p-8" aria-label={`Instruções para ${platformName(connection.platform)}`}><div><p className="eyebrow">Próximo passo</p><h2 className="mt-1 font-editorial text-2xl font-bold text-stone-900">Preparar o {platformName(connection.platform)}</h2><p className="mt-2 text-sm leading-relaxed text-stone-600">{localUrl ? 'Este ambiente usa um endereço local. Para usar um host externo, primeiro será necessária uma URL pública.' : 'Copie o endereço e use-o somente se o host da sua conta apresentar uma opção de conexão MCP ou ferramenta equivalente.'}</p></div><div className="rounded-xl border border-stone-200 bg-stone-950 p-3 font-mono text-xs text-stone-100 sm:flex sm:items-center sm:justify-between sm:gap-3"><code className="block overflow-x-auto py-1">{connection.mcpUrl}</code><button type="button" onClick={onCopyUrl} disabled={localUrl} className="mt-2 inline-flex min-h-11 items-center gap-2 rounded-lg bg-white/10 px-3 py-2 font-sans text-xs font-semibold text-white hover:bg-white/20 disabled:cursor-not-allowed disabled:opacity-50 sm:mt-0" aria-live="polite">{copied ? <Check className="h-4 w-4 text-emerald-300" aria-hidden="true" /> : <Copy className="h-4 w-4" aria-hidden="true" />}{copied ? 'URL copiada' : 'Copiar URL MCP'}</button></div>{localUrl && <p role="note" className="rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900">Esta URL é local e não pode ser usada pelo ChatGPT ou Claude. Para conectar um host externo, é necessário um endereço público do ForgeLex.</p>}<div className="grid gap-5 lg:grid-cols-2"><div><h3 className="text-sm font-bold text-stone-800">Requisitos do host</h3><ul className="mt-3 space-y-2 text-sm leading-relaxed text-stone-600">{connection.requirements.map((requirement) => <li key={requirement} className="flex gap-2"><Check className="mt-0.5 h-4 w-4 shrink-0 text-cognac-700" aria-hidden="true" />{requirement}</li>)}</ul></div><div><h3 className="text-sm font-bold text-stone-800">Configuração manual</h3><ol className="mt-3 list-decimal space-y-2 pl-5 text-sm leading-relaxed text-stone-600"><li>Abra a área de conectores, ferramentas ou MCP no {platformName(connection.platform)}.</li><li>Adicione a URL do ForgeLex e siga as confirmações solicitadas pelo host.</li><li>Quando houver verificação disponível, confirme o acesso antes de considerar a conexão concluída.</li></ol></div></div></section>;
};
