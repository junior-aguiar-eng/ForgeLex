# SDD ledger — plan: docs/superpowers/plans/2026-09-20-jurisprudence-normalization.md

Ruling: execute in the existing dirty `main` checkout — the user explicitly directed immediate repair here and a worktree would omit the uncommitted Phase 1 implementation; cost if wrong: overlap with preexisting edits, mitigated by preserving and reviewing every modified file.
Ruling: no per-task commits — user expressly prohibited commits; cost if wrong: no atomic Git checkpoints until explicit authorization.
Pre-flight: Task 1 produces fail-closed data-plane and explicit-cost invariants consumed by Tasks 2–4; Task 2 produces current-version serving consumed by Task 4; Task 3 produces manifest linkage and scalable publication consumed by Task 4.
Task 1: complete (no commit by user constraint; tests: `pnpm exec vitest run packages/billing-ledger/src/ledger.test.ts apps/api/src/app.test.ts apps/api/src/provider-parity.test.ts` → 45/45 pass). Ruling: `StjSconProvider` configuration remains for technical source use, but all app-created research services require persistence; cost if wrong: a deliberately fixture-only service must opt out explicitly.
Task 2: complete locally. PostgreSQL `0017` normalized the existing corpus; root documents retain only identity and search projection, while the current version provides legal payload and provenance. Integrity: 874450 documents, 874516 versions, 0 broken current pointers, 0 current-hash mismatches.
Task 3: complete locally. PostgreSQL `0018` links newly published versions to manifests and `0019` adds explicit publication state; publication is keyset-paginated in bounded batches. The regression test proves a pending manifest in an ultimately failed run is not commercially visible beside a completed manifest from the same run.
Task 4: local compacting cleanup complete for `jurisprudence_documents`: `VACUUM FULL` reduced its total relation size from 7739 MB to 3571 MB and the PostgreSQL data directory from 11.9 GB to 7.8 GB, after the integrity gate. The persisted repository query returned STJ Open Data documents after migration; broader REST/MCP benchmark and final validation remain.
